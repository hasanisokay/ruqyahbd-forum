import LoadingNoticePublic from "./LoadingNoticePublic";

const loadingNotice = () => {
    return (
        <div>
          <LoadingNoticePublic/>
        </div>
    );
};

export default loadingNotice;